/// Kind of media a [PhotoItem] represents.
///
/// `video` is reserved for a later (P2) extension of the slideshow to play
/// short video clips; it is not consumed anywhere yet in this scaffold.
enum MediaType {
  image,
  video,
}
