<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Found on a real device: NONE of these except location were ever
         declared, despite docs/PLAN.md's "Plattform-Besonderheiten" section
         listing exactly this set as required. Without a manifest
         declaration, Android silently denies the runtime permission
         request instead of showing the system prompt at all - CAMERA is
         needed for mobile_scanner's QR pairing scan (pairing_scan_screen.dart)
         and image_picker's camera option (upload_screen.dart);
         READ_MEDIA_IMAGES (API 33+)/READ_EXTERNAL_STORAGE (below 33) for
         image_picker's gallery option and file_picker's legacy (non-SAF)
         path on older Android versions; INTERNET/ACCESS_NETWORK_STATE/
         ACCESS_WIFI_STATE for every relay-server, SMB, Nextcloud and
         weather network call the app makes - without INTERNET specifically,
         literally no socket in this app could ever have connected. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <!-- Only requested if the user opts in to "aktuellen Standort verwenden" in
         the weather settings screen; manual city entry (default) needs no
         location permission at all. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <application
        android:label="PhotoFrame"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Registers this Activity as selectable as the device's Home
                 app/launcher (see docs/DECISIONS.md ADR-004: Kiosk/Autostart
                 limits). This is what actually gets the app to reappear
                 automatically after boot on Android - a bare BOOT_COMPLETED
                 receiver cannot start an Activity from the background since
                 API 29. The user must still explicitly confirm the "use as
                 Home app" system dialog the first time the Home button is
                 pressed after this is enabled (see kiosk_settings_screen.dart)
                 - there is no way to force that choice programmatically. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.HOME"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
